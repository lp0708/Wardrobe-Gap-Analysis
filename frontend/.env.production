# Public URL of the deployed FastAPI backend (Railway). Not a secret - it is
# baked into the built JavaScript. Override with a Vercel env var if it changes.
VITE_API_URL=https://wardrobe-gap-analysis-production.up.railway.app
